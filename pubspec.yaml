name: wifi_manager
description: WiFi Manager for Huawei HG531 V1 Router
version: 1.1.0+2
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  # الحل السحري: ترك الإصدارات فارغة لحل التعارض تلقائياً
  intl:
  mockito:
  bloc_test:
  webview_flutter: ^4.10.0
  dio: ^5.7.0
  flutter_bloc: ^8.1.6
  hive_flutter: ^1.1.0
  get_it: ^8.0.3
  # أضف باقي المكتبات بنفس الطريقة إذا استمر الخطأ
